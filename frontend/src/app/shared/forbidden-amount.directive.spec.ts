import { ForbiddenAmountDirective } from './forbidden-amount.directive';

describe('FrobiddenAmountDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenAmountDirective();
    expect(directive).toBeTruthy();
  });
});
